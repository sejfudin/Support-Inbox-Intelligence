// Placeholder rows/columns/cards to map over. Holes, not values — the callers
// only ever use the index, so there is nothing to fill in.
export const buildArray = (length) => Array.from({ length });
